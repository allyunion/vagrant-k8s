apiVersion: v1
kind: PeristentVolume
metadata:
  name: nfs-pv-$(hostname -s)
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/srv/k8s-nfs"
